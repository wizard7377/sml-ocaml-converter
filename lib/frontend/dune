(ocamllex lexer)

(library
 (name frontend)
 (libraries ast common menhirLib)
 (modules tokens utils lexer parser))

(menhir
 (modules parser)
 (flags --comment -la 0 --trace -lc 0 --dump --dump-resolved)
 ; (flags --dump)
 (explain true))
